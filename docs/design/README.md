# Design reference

This directory mirrors the Claude Design project "Design System" into the repo, so agents that can't reach claude.ai still get the visual source of truth from a checkout.

**Everything here except this README is written by a sync.** Don't hand-edit it — the next sync overwrites it. To change a design, change it in claude.ai/design and re-sync.

The sync procedure, and what to build from when the mirror doesn't cover what you need: `docs/agents/design.md`.

## Status

**Empty — last checked 2026-08-08.** The design project is created and bound, but nothing has been published to it, so there is nothing to mirror yet.

Unmirrored files (too large for the 256 KiB fetch cap): none.

_A sync rewrites this block; see step 4 of the procedure._
