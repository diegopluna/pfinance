// SVG text is drawn outside the CSS theme, so a chart's axis labels have to
// name the family the rest of the app gets from `--font-mono` in
// src/global.css. Keep the two in step: figures are monospaced everywhere,
// including inside a chart frame.
export const CHART_LABEL_FONT = 'SplineSansMono_400Regular'
